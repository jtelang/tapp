<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <!-- <item name="android:actionBarTabStyle">@style/styleActionBarTab</item> -->
        <!-- <item name="actionBarTabStyle">@style/styleActionBarTab</item> -->
    </style>

    <style name="styleActionBarTab" parent="@style/Widget.AppCompat.ActionBar.TabBar">
        <item name="android:showDividers">middle</item>
        <item name="android:gravity">center</item>
        <!-- <item name="android:background">@drawable/tab_bar_background</item> -->
        <!-- <item name="android:divider">@drawable/actionbar_tab_divider</item> -->
        <!-- <item name="android:dividerPadding">0dp</item> -->
        <!-- <item name="android:measureWithLargestChild">true</item> -->

    </style>

    <style name="Theme_CustomDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@android:color/transparent</item>
    </style>

</resources>